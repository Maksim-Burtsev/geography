Посты
- потыкать ещё раз редактор с картинками (и мб его оставить и вручную задавать ширину)


Тесты

- наполнить тесты

Форум

*авторизованный пользователь
    -сразу пишет сообщение

*неавторизованный
    -указывает имя и почту
    -почта валидируется на существование
    
- у комментариев хранить дату и сортировать по ней

Авторизация

Регистрация

-можно добавить капчу сюда

Погода у меня дома

-достать ip пользователя 
-по ip через API достать нужные данные
url = 'http://ip-api.com/json/'
    ip = '91.222.51.167'

    response = requests.get(f'{url}{ip}')

    response_json = response.json()

    country = response_json.get('country')
    city = response_json.get('city') 

    print(f'Страна {country}, город {city}')
-по данным получить погоду у пользователя
from pyowm import OWM

owm = OWM('812e41f6b01cf011959ab152d59ab3b9')
mgr = owm.weather_manager()

observation = mgr.weather_at_place('Simferopol')
w = observation.weather

temp_json = w.temperature('celsius')

temp = temp_json.get('temp')
temp_feel = temp_json.get('feels_like')

print(f'Температура {round(temp)}℃, ощущается как {round(temp_feel)}℃')
-вернуть общие данные о местоположении + погоду в этом месте
